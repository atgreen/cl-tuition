name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Extract version number
        id: get_version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Create source tarball
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          cd ..
          tar czf tuition-${VERSION}.tar.gz --transform "s,^cl-tuition,tuition-${VERSION}," \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='*.txt' \
            --exclude='*.lisp-temp' \
            --exclude='data/' \
            --exclude='.tools/' \
            cl-tuition
          mv tuition-${VERSION}.tar.gz cl-tuition/

      - name: Create GitHub release
        uses: ncipollo/release-action@339a81892b84b4eeb0f6e744e4574d79d0d9b8dd # v1.21.0
        with:
          tag: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          bodyFile: doc/release-notes/RELEASE-NOTES-${{ steps.get_version.outputs.version }}.md
          artifacts: |
            tuition-${{ steps.get_version.outputs.version }}.tar.gz
          allowUpdates: true
